name: Build

on:
  push:
    branches:
      - 'main'
  pull_request:
    branches:
      - '*'

concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  build:
    name: Run build on ${{ matrix.os }} - Node ${{ matrix.node-version }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest]
        node-version: ['22', '24']
        # os: [ubuntu-latest, windows-latest, macos-latest]
    env:
      # Increase Node.js heap size for large builds (embed package bundles all deps)
      NODE_OPTIONS: '--max-old-space-size=8192'
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node-version }}
      - name: Install the npm dependencies
        run: |
          npm install
      - name: Run the build
        run: |
          # One package at a time: jupyter-react's marimo bundle, its webpack
          # build and jupyter-embed's vite build each want several GB.
          npx lerna run build --concurrency 1
      - name: Build with Vite
        run: |
          cd packages/react && npm run build:vite
