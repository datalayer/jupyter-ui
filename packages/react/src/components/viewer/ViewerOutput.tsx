import { INotebookContent } from '@jupyterlab/nbformat';

type Props = {
  nbformat: INotebookContent
}

const ViewerOutput = (props: Props) => {
  return (
    <></>
  );
}

export default ViewerOutput;
